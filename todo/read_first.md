Я положил в проект папку с файлами \wb\examples из двух weather-ботов для Polymarket.
Твоя задача сейчас НЕ писать новый код сразу, а сначала внимательно изучить эти файлы и понять:

1) как в этих ботах находятся и подключаются погодные рынки Polymarket;
2) как из ответа Polymarket/Gamma извлекаются market id, token id, outcomes, цены, диапазоны температур;
3) как организовано локальное хранение данных по каждому рынку;
4) какие части логики можно переиспользовать для нового weather research bot, а какие нельзя.

ВАЖНО:
- сначала проведи анализ файлов в папке;
- не фантазируй про структуру API, а опирайся только на найденный код;
- если в коде есть несколько вариантов реализации, сравни их;
- прежде чем что-то менять, покажи мне краткий технический разбор.

Что нужно сделать:

A. Найти и изучить в этих файлах:
- функции/классы, связанные с Gamma API / Polymarket API;
- поиск event по slug;
- разбор списка markets внутри event;
- извлечение market id / clob token ids / outcome prices;
- парсинг температурных бакетов из question;
- функции сохранения и загрузки market JSON;
- структуру snapshot-логики.

B. После анализа выдай отчет в таком формате:

## 1. Как бот находит weather market
Опиши пошагово:
- как формируется slug;
- какой endpoint вызывается;
- что именно ожидается в ответе;
- как выбирается event;
- как из event извлекаются отдельные бакеты.

## 2. Какие поля надо сохранить для одного рынка
Составь список полей, которые реально нужны для research/stats bot.
Раздели на:
- market metadata
- forecast snapshots
- market snapshots
- derived fields

## 3. Как устроено текущее сохранение данных
Опиши:
- naming convention файлов;
- один файл на рынок или нет;
- append-only ли snapshots;
- какие массивы обновляются на каждом цикле;
- какие поля относятся к trading, а какие можно убрать в новой версии.

## 4. Что переиспользовать в новом боте
Раздели на:
- reuse as-is
- reuse with cleanup
- do not reuse

## 5. Предлагаемая минимальная структура нового research-only weather bot
Предложи структуру модулей/файлов для нового проекта, но БЕЗ реализации на этом шаге.

Очень важно:
- новый бот должен быть research/data-collection first;
- на этом этапе не нужен трейдинг, не нужен order execution, не нужны buy/sell функции;
- нужно только discovery weather markets, forecasts, snapshots, storage, later analytics;
- если в старом боте есть торговые поля (position, pnl, orderid, stoploss и т.д.), отметь их как legacy trading fields.

Дополнительные требования:
- если найдешь реальный паттерн хранения JSON, сохрани совместимость по духу: один рынок = один файл;
- если найдешь удачные helper-функции, перечисли их по именам;
- если какие-то поля выглядят лишними или хрупкими, отдельно пометь это;
- не пиши код, пока я не подтвержу следующий шаг.

Файлы, которые нужно изучить:
- все Python-файлы в папке
- все JSON-файлы-примеры рынков в папке

Ожидаемый результат сейчас:
только анализ, структурированный отчет и список рекомендаций.