Нужно скрыть несколько колонок только во frontend-таблице, НЕ удаляя их из данных, типов, API, моделей и JSON.
скрыть колонки только на UI-уровне, не удалять из типов, данных, API и моделей

Цель:
- данные должны продолжать приходить с backend как раньше
- поля должны оставаться в TypeScript типах
- поля должны оставаться доступны в row object
- просто не показывать эти колонки в таблице/UI по умолчанию

Сделай аккуратно, без лишнего рефакторинга.

Что нужно сделать:
1. Найди компонент таблицы, где описан список колонок (например MarketTable.tsx или рядом).
2. Спрячь ненужные колонки только на уровне отображения:
   - либо убери их из visibleColumns / renderedColumns
   - либо добавь им флаг hidden: true / visible: false
   - либо исключи их из дефолтного column definition, но не удаляй сами поля из типов
3. НЕ удаляй:
   - поля из frontend/src/types/market.ts
   - поля из backend-моделей
   - поля из API response
   - поля из stored JSON
4. Если в таблице есть отдельная конфигурация колонок, сделай так, чтобы:
   - колонка существовала
   - но по умолчанию была скрыта
   - при желании позже ее можно было легко вернуть
5. Не менять бизнес-логику.
6. Не ломать сортировку, фильтрацию и row mapping для остальных колонок.
7. Покажи:
   - какие файлы изменил
   - какие колонки теперь скрыты
   - за счет какого механизма они скрыты
   - полный итоговый код измененного компонента

Важно:
- hide, don’t delete
- data model must stay intact
- no backend changes unless absolutely required